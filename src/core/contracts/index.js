const TextProvider = require('./TextProvider');
const FileSystemPort = require('./FileSystemPort');
const DialogPort = require('./DialogPort');
const CommandRegistry = require('./CommandRegistry');

module.exports = {
  TextProvider,
  FileSystemPort,
  DialogPort,
  CommandRegistry,
};

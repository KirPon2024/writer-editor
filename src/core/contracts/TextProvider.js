/**
 * TextProvider contract.
 *
 * Source of text: editor engine via IPC (implementation is external).
 *
 * @returns {Promise<string>}
 */
async function getText() {}

module.exports = {
  getText,
};
